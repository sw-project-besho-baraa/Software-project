/**
 * Data Transfer Objects (DTOs) related to user information.
 * <p>
 * DTOs are simple carriers without behavior used to cross layer boundaries and
 * to shape inputs/outputs. Keep them immutable where practical and validate at
 * the edges.
 * </p>
 */
package DTO.UserDTO;